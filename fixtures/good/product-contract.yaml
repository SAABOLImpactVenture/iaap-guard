fixture:
  context: consumer-contract
  expectedCriticalFailures: 0
---
apiVersion: apiextensions.crossplane.io/v2
kind: CompositeResourceDefinition
metadata:
  name: applicationenvironments.platform.example.org
spec:
  group: platform.example.org
  names:
    kind: ApplicationEnvironment
    plural: applicationenvironments
  versions:
    - name: v1alpha1
      served: true
      referenceable: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              required: [cloud, region, owner, costCenter]
              properties:
                cloud:
                  type: string
                  enum: [aws, gcp]
                region:
                  type: string
                owner:
                  type: string
                  minLength: 3
                costCenter:
                  type: string
                  minLength: 3
