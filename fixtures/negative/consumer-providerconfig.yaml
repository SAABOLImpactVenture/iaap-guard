fixture:
  context: consumer-contract
  expected:
    ruleId: IAP-P001
    result: FAIL
---
apiVersion: platform.example.org/v1alpha1
kind: InfrastructureProductSchema
metadata:
  name: application-environment
spec:
  properties:
    cloud:
      type: string
    region:
      type: string
    owner:
      type: string
    providerConfig:
      type: string
      description: Consumer chooses the Crossplane ProviderConfig used for provisioning.
