fixture:
  context: experience
  expected:
    ruleId: IAP-X001
    result: FAIL
---
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: unsafe-infrastructure-order
spec:
  owner: platform-team
  type: infrastructure
  parameters:
    - title: Product request
      required: [name, cloud]
      properties:
        name:
          type: string
        cloud:
          type: string
          enum: [aws, gcp]
  steps:
    - id: provision
      name: Provision directly from storefront
      action: kubernetes:apply
      input:
        manifestPath: ./rendered/provider-resource.yaml
